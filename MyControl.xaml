<UserControl x:Class="AugustaStateUniversity.SeniorCapstoneIgnoreList.MyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vsfx="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.10.0"
             mc:Ignorable="d"              
             d:DesignHeight="300" d:DesignWidth="300"
             x:Name="MyToolWindow"
             Background="{DynamicResource {x:Static vsfx:VsBrushes.ToolWindowBackgroundKey}}" Loaded="MyToolWindow_Loaded" DataContext="{Binding}">
    <StackPanel Height="282" x:Name="stackPanel1" Width="281">
        <StackPanel Height="54" x:Name="stackPanel2" Width="264" Orientation="Horizontal" Margin="0">
            <TextBox Height="23" x:Name="ignoreListTextBox" Width="120" MinWidth="150" Margin="10" />
            <Button Content="Add" Height="23" x:Name="ignoreListAddButton" Width="75" Margin="10" IsManipulationEnabled="False" Click="ignoreListAddButton_Click" IsDefault="True" />
        </StackPanel>
        <ListView Height="209" x:Name="ignoreList" Width="262" AllowDrop="False" BorderThickness="0" />
    </StackPanel>
</UserControl>
